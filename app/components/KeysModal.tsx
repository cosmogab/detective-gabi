/**
 * Bring-your-own-key. Values live in `sessionStorage` only, are sent per request as headers,
 * and are never persisted server-side, never logged, never put in a URL. Inputs are masked
 * and each service shows its own status.
 *
 * Props are a starting point for the owning lane to refine; the file exists so that no two
 * lanes create it.
 */
export function KeysModal(props: { open: boolean; onClose: () => void }) {
  throw new Error('not implemented')
}
